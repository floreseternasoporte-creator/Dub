# ── Web framework ──────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
pydantic==2.7.1
aiofiles==23.2.1
python-dotenv==1.0.1

# ── HTTP client para llamar a Modal ───────────────────────────────────
httpx==0.27.0

# ── Audio/Video (Railway — solo ffmpeg + whisper, NO torch pesado) ────
ffmpeg-python==0.2.0
soundfile==0.12.1
numpy==1.26.4

# ── Transcripción local (Whisper base — ~150 MB, corre en CPU) ────────
faster-whisper==1.0.3

# ── Base64 ya incluida en stdlib — no necesita pip ────────────────────
